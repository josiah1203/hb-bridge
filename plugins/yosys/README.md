# Yosys bridge (Phase 0)

**Status:** harness-only — see [`../README.md`](../README.md).

Headless mapping in `crates/hnf-phase0-tools`; corpus `corpora/yosys/`, fixture `tests/fixtures/yosys/minimal_counter.json`.
