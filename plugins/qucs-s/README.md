# Qucs-S bridge (Phase 0)

**Status:** harness-only — see [`../README.md`](../README.md).

Headless mapping lives in `crates/hnf-phase0-tools`; corpus under `corpora/qucs-s/`. Upstream in-tool plugin planned (Week 16).
