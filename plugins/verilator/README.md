# Verilator bridge (Phase 0)

**Status:** harness-only — see [`../README.md`](../README.md).

Corpus: `corpora/verilator/`. Host smoke: `HB_BRIDGE_HOST_VERILATOR=1` + `cargo test -p roundtrip-harness -- --ignored`.
