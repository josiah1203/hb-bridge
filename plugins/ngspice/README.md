# ngspice bridge (Phase 0)

**Status:** harness-only — see [`../README.md`](../README.md).

Headless mapping in `crates/hnf-phase0-tools`; corpus `corpora/ngspice/`, fixture `tests/fixtures/ngspice/rc_lowpass.json`.
