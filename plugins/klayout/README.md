# KLayout bridge (Phase 0)

**Status:** harness-only — see [`../README.md`](../README.md).

Headless mapping lives in `crates/hnf-phase0-tools`; corpus under `corpora/klayout/`. Upstream in-tool plugin not started.
