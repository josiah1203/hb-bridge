# PlatformIO bridge (Phase 0)

**Status:** harness-only — see [`../README.md`](../README.md).

Headless mapping lives in `crates/hnf-phase0-tools`; corpus under `corpora/platformio/`. Upstream VS Code extension / CLI integration planned (Week 16).
